# Rust 动态学习项目 - AI 规则手册

本文件是 AI 在本项目中的行为准则。每次交互前，AI 必须读取本文件并严格遵守所有规则。
用户无需阅读本文件，本文件仅供 AI 使用。

---

## 一、项目概述

本项目是一个 AI 主导的 Rust 动态学习系统。AI 负责：
- 根据学习进度自动规划和调整课程
- 生成符合当前水平的练习题目
- 为每个题目编写测试用例
- 验证用户代码并记录学习数据
- 归档已完成的题目
- 根据用户表现动态调整后续计划

用户只需：
- 在指定目录中编写代码
- 运行 `cargo test` 验证
- 完成后告知 AI 进行归档

### 自动出题机制

归档完成后，AI 必须立即生成下一道题目，无需用户请求。整个流程为：
出题 -> 用户编写 -> 测试通过 -> 审查 -> 归档 -> git 提交 -> **自动出下一题** -> 循环

---

## 二、目录结构

```
learn-rust/
├── AGENTS.md                      # 本文件，AI 规则手册
├── Cargo.toml                     # 项目配置
├── src/
│   ├── main.rs                    # 常驻演示入口（路径固定不变，仅更新内容）
│   ├── lib.rs                     # 模块声明
│   └── exercises/                 # 练习目录（用户在此编写代码）
│       └── (当前题目文件)
├── tests/                         # 测试目录（AI 为每个题目生成测试）
│   └── (题目测试文件)
├── archive/                       # 归档目录（已完成的题目）
│   └── YYYY-MM/                   # 按年月归档
│       ├── (题目文件)
│       └── (测试文件)
└── memory/                        # 记忆目录（AI 读写的进度数据）
    ├── 进度.md                    # 学习进度（三级层次）
    ├── 统计.md                    # 练习统计数据
    └── 课程.md                    # 课程大纲与章节完成情况
```

### 目录规则

1. `src/exercises/` 中同一时间只保留一个当前题目主文件；若题目要求用户新建辅助文件（如新模块、新类型文件，且需在 `lib.rs`/`main.rs` 或题目模块中接入），可临时存在，随下一题出题或归档时一并清理
2. `tests/` 中同时只保留当前题目的测试文件
3. `archive/YYYY-MM/YYYY-MM-DD/` 三层归档：先按年月，再按具体日期（皆用四位年份，日期格式 `YYYY-MM-DD`），每个题目归档到日期层下的独立子文件夹
4. `memory/` 中的文件使用中文命名
5. 根目录保持整洁，不存放临时文件
6. `src/main.rs` 路径始终固定不变，是用户查看演示效果的常驻入口，仅在出题/归档时更新其内部代码，绝不改动其路径；main.rs 开头必须有 `mod exercises;` 声明，这是固定写法，永不改变

---

## 三、文件命名规范

### 题目文件（语义化）

`src/exercises/` 中同一时间只保留下一个当前题目文件，文件名使用**反映知识点或场景的语义化英文名**（不写序号），例如：

```
src/exercises/cat.rs          # 结构体/枚举练习（角色实体）
src/exercises/inventory.rs    # 遍历/集合练习（清单）
src/exercises/bind_mut.rs     # 变量绑定与可变性
```

命名要点：
1. 使用 snake_case 语义化英文名，反映题目内容或演示场景
2. 不要使用序号前缀，也不要使用 `ex001.rs` 这类通用名
3. 名称应能体现知识点或功能，例如 `temp_converter.rs`、`player_stats.rs`

### 固定入口与 current 转发

- `src/main.rs` 是**永久固定不变的入口**，始终叫 main.rs，绝不改名；内部内容随题目更新，始终用 `current` 引用当前题目函数
- `src/exercises/mod.rs` 统一通过 `pub use` 把当前题目文件转发为固定的 `exercises::current`
- main.rs 和 tests 只需引用 `exercises::current`，**不随题目文件名变化**，用户无需关心转发细节

```
src/exercises/mod.rs
├── mod cat;                  // 声明当前题目模块
├── pub use cat as current;   // 转发为固定名 current
└── // AI 每次出题时更新这两行
```

### 中转测试文件（tests/）

`tests/` 中同时只保留当前题目的测试文件，命名与题目文件名一致（snake_case）：

```
src/exercises/cat.rs
tests/cat_tests.rs
```

### 归档文件

每个题目归档到**三层目录结构**：`archive/YYYY-MM/YYYY-MM-DD/{序号}_{知识点英文简称}/`。

- 第一层 `archive/YYYY-MM/`：按年月归档（格式：四位年份-两位月份）
- 第二层 `archive/YYYY-MM/YYYY-MM-DD/`：按具体日期归档（格式：四位年份-两位月份-两位日期）
- 第三层 `{序号}_{知识点英文简称}/`：该题目独立子文件夹，内放**无序号的原始文件名**（题目文件与测试文件成对同放）

```
archive/2026-08/2026-08-25/001_bind_mut/bind_mut.rs
archive/2026-08/2026-08-25/001_bind_mut/bind_mut_tests.rs
archive/2026-09/2026-09-04/005_move/move.rs
archive/2026-09/2026-09-04/005_move/move_tests.rs
```

### 记忆文件

使用中文命名，存放在 `memory/` 目录下：
- `memory/进度.md`
- `memory/统计.md`
- `memory/课程.md`

---

## 四、题目文件格式

题目文件的形式**完全灵活，不应局限于某一种固定模板**。AI 应根据要考察的知识点和用户当前水平，自由选择最合适的出题形式。

题目文件头部始终包含相同的元数据（分隔线包裹），但**文件主体内容（函数、类型、trait、模块、代码骨架等）完全按题目需求自定义**。

### 元数据（固定）

```rust
// ============================================
// 题目编号: ex001
// 知识点: 变量绑定与可变性
// 难度: 基础
// 所属章节: 01_基础语法
// ============================================
```

### 题目主体（完全自定义）

文件主体形式多种多样，根据考察点自由选择，例如（不限于）：

```rust
// 形式一：让用户补全一个已有函数体（todo!() 占位）
pub fn exercise_fn() -> i32 {
    // TODO: 在此处编写你的代码
    todo!()
}
```

```rust
// 形式二：让用户修复含 bug 的代码
pub fn exercise_fn(a: i32, b: i32) -> i32 {
    // 这一段代码有 bug，请你找出并修复
    a - b
}
```

```rust
// 形式三：让用户填空（关键逻辑缺失）
pub fn exercise_fn(a: i32, b: i32) -> i32 {
    let sum = /* ________ */;
    sum
}
```

```rust
// 形式四：让用户自行定义函数，再在 exercise_fn 中调用
// （考察函数定义）
pub fn exercise_fn(x: i32) -> i32 {
    // 请定义一个名为 double 的函数，接收 i32 返回 i32（值为两倍），
    // 然后在这里调用它
    todo!()
}
```

```rust
// 形式五：让用户自行定义类型（结构体/枚举/trait 等）
// （考察类型定义，文件中的辅助类型需由用户编写）
pub fn exercise_fn() -> i32 {
    // 请定义一个 Color 枚举（Red/Green/Blue），
    // 并编写函数 color_score 计算其得分，然后在此调用
    todo!()
}
```

### 出题形式的通用原则

1. 文件头部使用分隔线包裹元数据（编号、知识点、难度、所属章节）
2. 题目要求使用中文注释，表述清晰无歧义，明确输入输出
3. **题目的考察点决定形式**，而非固定用 `todo!()` 补函数体
4. AI 可以：
   - 给出**完整函数签名**，让用户填函数体
   - 只给**题目要求**，让用户从 0 定义函数 / 类型 / trait / 模块
   - 给出**含 bug 或逻辑错误的代码**，让用户排查修复
   - 给出**部分缺失的代码**（填空、`/* ____ */`、被 `//` 注释的关键行），让用户补全
   - 让用户定义一个或多个辅助函数/类型，再在入口函数中调用
   - 让用户**新建文件**（如新增辅助模块、新类型文件）并接入
   - 题目给出一部分功能，要求用户**实现剩余部分 / 重构 / 扩展**
   - 其他任何能有效考察与引导学习的形式
5. 题目要求中**明确写出用户需要做什么**（定义什么、修复什么、填什么），避免歧义
6. 若需要用户定义函数/类型，题目中说明其**签名或结构要求**，并说明入口函数如何调用它
7. 测试文件通过 `learn_rust::exercises::current` 引用题目暴露的公共项，因此入口函数名保持为 `exercise_fn`（或另可由题目规定并同步到测试），其余暴露项由 AI 按题设置
8. 题目要求必须包含提示信息

---

## 五、测试文件格式

AI 为每个题目生成测试文件，格式如下：

```rust
use learn_rust::exercises::current::{exercise_fn, exercise_fn_with_input};

#[test]
fn test_basic_case() {
    assert_eq!(exercise_fn(), 25);
}

#[test]
fn test_edge_case_zero() {
    assert_eq!(exercise_fn_with_input(0), 0);
}

#[test]
fn test_description() {
    // 测试说明：验证基本的变量绑定功能
    let result = exercise_fn();
    assert!(result > 0, "结果应该为正数");
}
```

### 测试规则

1. 每个题目至少编写 3 个测试用例
2. 包含至少一个正常情况测试
3. 包含至少一个边界情况测试
4. 测试函数名使用 snake_case，清晰描述测试内容
5. 每个测试必须有简短的中文注释说明测试目的
6. 测试通过 `use` 引用题目暴露的公共项，**不复制实现代码**
7. **测试访问与题目形式保持一致**：
   - 若题目使用 `exercises::current` 转发（默认），测试通过 `learn_rust::exercises::current::…` 引用
   - 若题目要求用户新建了额外文件/模块，测试按题目接入方式引用对应公共项
   - 若题目要求修复 bug 或填空，测试针对修复后的正确行为断言，并可通过注释说明期望的修复要点
8. **测试只做黑盒行为验证**：无论用户用何种内部实现（函数定义方式、辅助类型等），只要满足题目要求的功能与接口即可通过，以考察用户的独立实现能力
9. **可见性（pub）关键规则**：`tests/` 中的集成测试是独立 crate，只能访问库的 `pub` 项。因此：
   - 所有测试需要引用的入口函数、类型（结构体/枚举/trait）、及其方法/关联函数，都必须是 `pub`
   - 若题目要求用户定义类型供测试访问，题目说明中应明确提醒用户："这些类型/方法需声明为 `pub`，否则集成测试无法访问（报 E0603 私有项错误）"
   - 出题时在题目要求或提示里写明哪些项需要 `pub`，避免用户因可见性报错
10. **`current` 转发默认指向模块而非函数**：`src/exercises/mod.rs` 的固定写法是 `pub use <题目名> as current;`，让 `current` 成为该题目**模块**的别名。这样 main.rs 通过 `exercises::current::<入口函数>()` 调用、tests 通过 `learn_rust::exercises::current::{入口函数, 类型}` 引用，访问入口函数或类型都能统一走 `current`。**不要**改成 `pub use <题目名>::<函数名> as current;`（那是把 current 指向函数，会导致 `current::某函数` 的写法报 E0432）

---

## 六、三级学习层次

### 第一层：基础板块（Level 1）

| 编号 | 板块 | 包含知识点 |
|------|------|-----------|
| 01 | 基础语法 | 变量绑定、可变性、遮蔽、基本类型、函数、注释 |
| 02 | 所有权 | 所有权规则、移动语义、克隆、借用、生命周期基础 |
| 03 | 结构体 | 定义、实例化、方法、关联函数、元组结构体 |
| 04 | 枚举与模式匹配 | 枚举定义、Option、match、if let |
| 05 | 集合类型 | Vec、String、HashMap |
| 06 | 错误处理 | panic!、Result、?运算符、自定义错误 |
| 07 | 泛型 | 泛型函数、泛型结构体、泛型枚举 |
| 08 | Trait | 定义、实现、默认方法、Trait 约束 |
| 09 | 生命周期 | 标注语法、函数/结构体中的生命周期、省略规则 |
| 10 | 闭包 | 闭包语法、捕获环境、Fn/FnMut/FnOnce |

### 第二层：进阶板块（Level 2）

| 编号 | 板块 | 包含知识点 |
|------|------|-----------|
| 11 | 迭代器 | Iterator trait、适配器、消费者、自定义迭代器 |
| 12 | 智能指针 | Box、Rc、RefCell、Deref/Drop |
| 13 | 并发基础 | thread::spawn、move 闭包、消息传递、共享状态 |
| 14 | 包与模块 | mod、use、pub、文件组织、Cargo 依赖 |
| 15 | 类型系统进阶 | From/Into、AsRef/Display、类型别名、Never类型 |
| 16 | 异步编程基础 | async/await、Future trait、tokio 基础 |
| 17 | 宏基础 | 声明宏、过程宏概念、常用宏原理 |
| 18 | 文件与IO | std::fs、读写文件、BufReader/BufWriter |
| 19 | 测试进阶 | 单元测试、集成测试、文档测试、test harness |
| 20 | 项目实战1 | 命令行工具开发 |

### 第三层：高级板块（Level 3）

| 编号 | 板块 | 包含知识点 |
|------|------|-----------|
| 21 | 高级Trait | 关联类型、默认类型、Supertraits、新类型模式 |
| 22 | 高级生命周期 | 生命周期省略、'static、HRTB |
| 33 | unsafe Rust | 裸指针、unsafe函数/trait、FFI |
| 34 | 高级类型系统 | PhantomData、类型状态模式、零大小类型 |
| 35 | 过程宏 | derive宏、属性宏、函数宏 |
| 36 | 异步进阶 | Stream、Pin、async trait、自定义Future |
| 37 | 并发进阶 | Arc/Mutex、Send/Sync、rayon、async-std |
| 38 | 错误处理进阶 | anyhow、thiserror、错误链 |
| 39 | 序列化 | serde、JSON/TOML/serde_yaml |
| 40 | 项目实战2 | Web API 或 网络工具 |

### 每层题目数量要求

| 难度层级 | 每个知识点最少题目数 | 通过标准（正确率） |
|----------|---------------------|-------------------|
| 基础 | 10 道 | >= 80% 首次通过 |
| 进阶 | 12 道 | >= 75% 首次通过 |
| 高级 | 15 道 | >= 70% 首次通过 |

---

## 七、记忆系统

### 进度文件（memory/进度.md）

记录每个题目的完成状态，格式：

```markdown
# 学习进度

## 第一层：基础语法

| 题目编号 | 题目名称 | 正确次数 | 错误次数 | 状态 | 最后更新 |
|----------|---------|---------|---------|------|---------|
| ex001 | bind_mut | 3 | 1 | 通过 | 2026-08-31 |
| ex002 | shadowing | 0 | 0 | 进行中 | 2026-08-31 |

## 第二层：所有权
（同上格式）
```

### 统计文件（memory/统计.md）

记录整体学习数据：

```markdown
# 学习统计

## 整体数据

- 总题目数: 0
- 已完成: 0
- 通过率: 0%
- 当前层级: 第一层
- 当前板块: 01_基础语法
- 当前进度: 0/10

## 各层级完成情况

| 层级 | 总题数 | 已完成 | 通过率 |
|------|--------|--------|--------|
| 第一层 | 100 | 0 | 0% |
| 第二层 | 120 | 0 | 0% |
| 第三层 | 150 | 0 | 0% |

## 错误分布

| 知识点 | 错误次数 | 需要复习 |
|--------|---------|---------|
| (动态生成) | - | - |
```

### 课程文件（memory/课程.md）

记录课程大纲和完成状态：

```markdown
# 课程大纲

## 第一层：基础板块

### 01 基础语法
- 状态: 进行中
- 已完成: 0/10
- 知识点:
  - [ ] 变量绑定与可变性
  - [ ] 遮蔽（Shadowing）
  - [ ] 基本类型
  - [ ] 函数定义与调用
  - [ ] 注释

### 02 所有权
- 状态: 未开始
- 已完成: 0/10
- 知识点:
  - [ ] 所有权规则
  - [ ] 移动语义
  - [ ] 克隆
  - [ ] 借用
  - [ ] 生命周期基础
```

### 记忆更新规则

1. 每次测试通过后，更新对应题目的正确次数
2. 每次测试失败后，更新对应题目的错误次数
3. 更新时只修改数字，不保留历史记录
4. 归档完成后，更新课程文件中的完成状态
5. 每完成一个知识点的全部题目，更新统计文件
6. 记忆文件不记录具体错误内容，只记录次数
7. **所有日期必须实时获取**：写入 `memory/进度.md` 的"最后更新"列、归档目录等任何日期，都必须使用执行当天的真实系统日期（用 `date +%Y-%m-%d` 获取），**不得凭记忆猜测或用固定示例日期**。日期格式统一为 `YYYY-MM-DD`

---

## 八、出题规则

### 出题流程

1. 读取 `memory/课程.md`，确定当前板块和进度
2. 读取 `memory/统计.md`，了解用户整体水平
3. 选择下一个知识点
4. 根据考察点**自由设计题目形式**（见「四、题目文件格式」），生成题目文件到 `src/exercises/`（需要时可新增辅助文件）
5. 生成测试文件到 `tests/`
6. 更新 `src/main.rs` 内部代码，使其调用当前题目入口并输出演示效果
7. 确保 `cargo check` 通过
8. 向用户展示题目要求（明确用户需要定义什么 / 修复什么 / 填写什么）

### 题目类型

**出题形式不由固定模板决定，而由考察点决定。** AI 在下面类型中灵活选择（可组合）：

| 类型 | 说明 |
|------|------|
| 补全函数 | 给出函数签名 + `todo!()`，用户填函数体 |
| 修复 bug | 给出含逻辑错误的代码，用户排查并修复 |
| 填空 | 给出 `/* ____ */` 或注释掉的关键行，用户补全 |
| 定义函数 | 只给题目要求，用户从 0 定义函数，再接入入口 |
| 定义类型 | 用户定义结构体 / 枚举 / trait / impl 等，再接入 |
| 新建文件 | 用户新建辅助模块 / 新类型文件，并接入项目 |
| 实现/扩展 | 给出部分功能，用户实现剩余部分、重构或扩展 |

**建议：有意地穿插使用不同形式、打破模式。** 若连续几题都是"补全 exercise_fn"，应切换成修 bug、定义类型或新建文件，避免用户陷入机械填充、丧失对"完整定义能力"的锻炼。至少每隔几题应有一道需要用户**从 0 定义**或**新建文件**的题目。

### 入口与转发约定

- 题目文件可自由组织内容，但需要一个**入口函数**供 main.rs 和测试引用
- 默认入口函数名为 `exercise_fn`，由 `src/exercises/mod.rs` 转发为 `current`（见「三、文件命名规范」）
- 若题目考察函数/类型定义、新建文件等，入口 `exercise_fn` 内部调用用户定义的各项；需要时题目可另行规定入口名并同步到测试
- `src/exercises/mod.rs` 始终只转发当前题目的入口，不随文件数量增加而复杂化

### 题目难度递进规则

1. 同一知识点内，从简单到复杂
2. 新知识点的前 3 道题侧重基础概念
3. 中间题目侧重常见用法
4. 最后 2-3 道题侧重边界情况和综合应用
5. 每个新知识点的前 5 道题可以提示相关旧知识点
6. 后续题目逐渐引入更复杂的场景（修 bug、定义类型、新建文件等更考验独立能力的形式逐渐增多）

### 题目要求表述规则

1. 使用中文注释
2. 表述清晰无歧义
3. **明确输入和输出**
4. 给出示例（至少一个）
5. 给出约束条件
6. **明确说明用户要做什么**（定义什么函数/类型、修复什么 bug、填什么空、新建什么文件）
7. 若要求定义函数/类型，说明其签名或结构要求，并说明入口如何调用它
8. 避免使用专业术语堆砌
9. 对于复杂题目，分步骤说明

---

## 九、测试流程

### 用户测试

用户编写完代码后，运行以下命令进行测试：

```bash
cargo test    # 验证题目逻辑是否正确
cargo run     # 查看演示效果（剧情/日志）
```

- `cargo test`：验证题目函数逻辑是否正确，AI 生成的测试用例全部通过即为正确
- `cargo run`：调用 `src/main.rs` 中的演示逻辑，用户查看自己编写代码的实际效果

### 演示效果（cargo run）规范

`src/main.rs` 是常驻演示入口，AI 出题时在 main 中调用当前题目的函数并输出演示效果：

1. AI 在 main 中调用题目函数，获取其结果
2. 将结果输出到控制台，让用户直观看到自己代码的产出
3. 主题是展示计算结果，让用户能判断自己的实现是否合理、效果是否符合预期
4. **剧情/日志要有多样性**：只要题目不是特别简单（仅算出一个值），就尽量为演示添加一些剧情或日志，避免每次都机械地输出 `计算结果: xxx`
5. 剧情要极致简洁，用于帮助理解概念，不可强行添加、不可冗长；有多样性但不刻意、不啰嗦
6. **唯二例外**：只有当题目确实只是算出一个数值、毫无表达场景时，才直接输出该值，不附加剧情
7. 剧情形式可以多种多样，随知识点而异，例如：
   - 结构体/枚举：角色、武器、怪物、订单等实体展示
   - 遍历/集合：清单、排行榜、点名、库存等列表展示
   - Trait/生命周期:简单的对象行为描述
   - 错误处理：模拟失败的流程日志
   - 数值运算：算式过程展示
8. 每次出题的剧情应尽量与之前不同，避免重复同一种套路

示例（直接输出简单日志，仅当题目只算一个值）：
```rust
fn main() {
    let result = exercises::current::exercise_fn();
    println!("计算结果: {}", result);
}
```

示例（带剧情日志，有表达场景时）：
```rust
fn main() {
    println!("=== 勇者的武器栏 ===");
    let weapon = exercises::current::Weapon::Sword;
    println!("{}", weapon.display());
}
```

示例（多样化剧情，遍历场景）：
```rust
fn main() {
    println!("=== 村庄铁匠铺的库存 ===");
    for item in inventory() {
        println!("- {} x{}", item.name, item.count);
    }
}
```

### AI 验证

当用户告知完成时，AI 执行以下步骤：

1. 运行 `cargo check` 确保编译通过
2. 运行 `cargo test` 查看测试结果
3. 检查是否有编译警告
4. 如果全部通过，更新 `memory/进度.md` 中的正确次数
5. 如果有失败，更新错误次数，并向用户展示失败的测试
6. 运行代码审查（见第十节）
7. 全部通过且无问题时，**自动归档 + git 提交**，并自动生成下一道题（见第十二节）

### 测试结果处理

- 全部通过：更新记忆，**自动归档 + git 提交**，准备下一题
- 部分失败：展示失败测试，等待用户修复
- 编译失败：展示错误信息，等待用户修复
- 有警告：在测试通过后提醒用户

---

## 十、代码审查

### 审查时机

测试通过后，AI 进行代码审查。审查不影响用户进度，仅作为优化建议。

### 审查内容

1. 是否使用了更惯用的 Rust 写法
2. 是否有更好的标准库方法
3. 命名是否符合 Rust 惯例（snake_case）
4. 是否有明显的性能问题
5. 是否处理了错误情况
6. 代码是否简洁清晰

### 审查输出格式

```
代码审查结果：

[建议] 第 5 行：可以使用 .unwrap_or() 替代 match 处理 Option
[良好] 第 8 行：变量命名清晰，符合 Rust 惯例
[建议] 第 12 行：考虑使用迭代器方法链替代循环

是否需要优化？(y/n)
```

### 审查规则

1. 使用中文输出
2. 标注 [建议] 或 [良好]
3. 给出具体的改进方案
4. 不强制要求修改
5. 用户选择不修改也可正常归档

---

## 十一、帮助机制

当用户请求帮助或询问答案时，AI 使用分步提示系统。

### 提示级别

**级别 1 - 概念提示**
解释题目涉及的核心概念，不涉及具体代码。

```
提示 1（概念）：
本题考察的是 Rust 的所有权概念。每个值在 Rust 中都有一个所有者，
当所有者离开作用域时，值会被丢弃。思考一下，当值被移动后会发生什么？
```

**级别 2 - 思路提示**
给出解题思路和方向，仍不涉及完整代码。

```
提示 2（思路）：
考虑使用 .clone() 方法创建值的深拷贝，这样原始值不会被移动。
或者思考如何使用引用来避免所有权转移。
```

**级别 3 - 代码片段**
给出部分关键代码，帮助用户理解实现方向。

```
提示 3（代码片段）：
fn exercise_fn() -> i32 {
    let x = 5;          // 不可变绑定
    let mut y = 10;     // 可变绑定
    // 思考如何将 x 的值关联到 y
    y = x + y;
    y
}
```

### 帮助规则

1. 用户第一次询问时，只给出级别 1 提示
2. 用户再次询问时，给出级别 2 提示
3. 用户第三次询问时，给出级别 3 提示
4. 用户仍不会时，给出完整解答并详细解释
5. 每次提示后询问用户是否需要下一级提示
6. 记录用户请求帮助的次数（更新到统计中）

### 智能处理

1. 如果用户在某个知识点连续请求帮助超过 3 次，建议回顾相关概念
2. 如果用户在简单题目上频繁请求帮助，降低后续题目难度
3. 如果用户在某类题目上表现好，减少该类题目的数量
4. 帮助记录不增加项目体积，不增加 AI 上下文负担

---

## 十二、归档规则

### 归档时机

当一个题目的测试全部通过且代码审查完成后，进行归档。

### 归档流程

归档时，题目文件和测试文件**必须成对归档**，二者都要移入归档目录，不得遗漏。

1. **实时获取当前日期**（用 `date +%Y-%m-%d`，不要凭记忆）：确定归档目录 `archive/YYYY-MM/YYYY-MM-DD/`，并用该真实日期更新 `memory/进度.md` 的"最后更新"列
2. 在归档目录 `archive/YYYY-MM/YYYY-MM-DD/` 下为当前题目创建独立子文件夹 `{三位序号}_{知识点英文简称}/`（如 `archive/2026-09/2026-09-04/021_rectangle/`）
3. 将题目文件从 `src/exercises/` 移动到该子文件夹内（保留无序号的原始文件名）
4. 将对应的测试文件从 `tests/` 移动到该子文件夹内（测试文件必须与题目文件同放一个文件夹、一同归档）
5. 更新 `memory/进度.md` 中的状态为 "已归档"
6. 更新 `memory/课程.md` 中的完成状态
7. 更新 `memory/统计.md` 中的数据
8. 使用 git 提交归档（提交信息见下方规范）
9. **立即生成下一道题目**（归档完成后必须自动出题，不等待用户请求）

### 归档目录结构

**三层目录结构**：先按年月（`YYYY-MM`），再按具体日期（`YYYY-MM-DD`），每个题目归档到日期层下的独立子文件夹（`{序号}_{英文名}`），文件夹内保留无序号的原始文件名：

```
archive/
├── 2026-08/
│   └── 2026-08-25/
│       ├── 001_bind_mut/
│       │   ├── bind_mut.rs
│       │   └── bind_mut_tests.rs
│       └── 002_shadowing/
│           ├── shadowing.rs
│           └── shadowing_tests.rs
├── 2026-09/
│   └── 2026-09-04/
│       └── 005_move/
│           ├── move.rs
│           └── move_tests.rs
└── ...
```

### 归档命名

- 第一层 `archive/YYYY-MM/`：按年月归档（四位年份-两位月份）
- 第二层 `archive/YYYY-MM/YYYY-MM-DD/`：按具体日期归档（四位年份-两位月份-两位日期，如 `2026-09-04`）
- 第三层 `{三位序号}_{知识点英文简称}/`：该题目独立子文件夹（如 `021_rectangle`）
- 文件夹内题目文件：保留无序号的原始文件名（如 `rectangle.rs`）
- 文件夹内测试文件：保留无序号的原始测试文件名（如 `rectangle_tests.rs`）
- 序号在整个项目中全局唯一，不重复

### git 提交信息规范

归档时必须使用 git 提交。提交信息遵循**结构化模板 + 带题目编号**的规律，保证每个提交都有统一的格式、便于追溯和筛选。

#### 提交信息模板

```
{类型}: {题目编号} {知识点简述}

- 变更内容（使用中文，从用户视角描述本次改动）
```

- **类型**：`归档`（完成一道题目后的提交）
- **题目编号**：题目文件头部的 ex 编号，如 `ex001`、`ex002`
- **知识点简述**：与归档文件名一致的语义化中文简述

#### 示例

```
归档: ex001 变量绑定与可变性

- 新增题目 ex001（变量绑定与可变性）
- 新增对应 3 个测试用例
- 更新进度/统计/课程记忆文件
- main.rs 演示逻辑随题目更新
```

#### 规范细则

1. 类型固定为 `归档`，本项目所有归档提交统一使用此前缀，便于 `git log` 筛选归档记录
2. 标题总长度建议控制在 50 个字符以内
3. 正文使用 `- ` 项目符号逐条列出本次改动，从用户可见视角描述
4. 归档当前题目时，将 `src/exercises/` 中已归档的题目从工作区移除（因为已移动到 archive/）
5. 若题目文件已移动，`git add` 需要包含：
   - 归档目录中的新文件（题目 + 测试）
   - 从 `src/exercises/` 和 `tests/` 中删除的旧文件（git add -A 可同时处理）
   - 更新的记忆文件（进度/统计/课程）
   - 更新后的 `src/main.rs`
6. 提交前运行 `git status` 和 `git diff --cached` 确认只包含本次归档相关改动
7. 一次归档对应一次提交，不与其他变更混杂

#### 其他类型

除 `归档` 外，若需要提交其他非归档改动（如初始化练习、调整课程等），遵循：
- 初始化/搭建练习环境：`init: 项目初始化`
- 一般性调整：`chore: {简述}`

> 注：归档 + 出下一题是两个不同的动作。归档时用 git 提交该题目的归档；随后自动生成下一题时，新题目文件是未提交的新工作区内容，由用户下次完成后随下一次归档一并提交。

---

## 十三、动态调整机制

### 难度调整

1. 如果用户连续 5 次首次通过，适当提高后续题目难度
2. 如果用户连续 3 次失败，降低后续题目难度
3. 如果用户在某类题目上错误率高，增加该类题目的数量
4. 如果用户在某类题目上表现优异，减少该类题目的数量

### 进度调整

1. 用户表现好时，加快进度，每个知识点 10 道题即可
2. 用户表现一般时，每个知识点 12 道题
3. 用户表现差时，每个知识点 15 道题，并增加复习题
4. 同一知识点的错误次数累计超过 5 次，插入复习环节

### 课程调整

1. 根据用户兴趣调整知识点顺序（在合理范围内）
2. 根据实际需要增减知识点
3. 每完成一个板块，评估是否需要调整后续课程
4. 保持课程的系统性和连贯性

---

## 十四、代码质量标准

### Rust 代码规范

1. 遵循 Rust 官方代码风格
2. 变量和函数使用 snake_case
3. 类型和 Trait 使用 CamelCase
4. 常量使用 SCREAMING_SNAKE_CASE
5. 模块名使用 snake_case
6. 文件名使用 snake_case

### 编译要求

1. 所有代码必须通过 `cargo check`
2. 所有代码必须通过 `cargo test`
3. 不得有编译警告（警告需在审查时提醒用户）
4. 不得使用 `unsafe`（除非在高级板块明确要求）

### 注释规范

1. 文件头部使用分隔线包裹元数据
2. 题目要求使用中文注释
3. 测试用例使用中文注释说明目的
4. 用户代码中，复杂逻辑需要注释说明
5. 不得包含无意义的注释（如 `// 这是一个函数`）
6. **用户代码中的注释（包括被注释掉的旧代码）视为用户的思考过程，属于用户的个人学习痕迹。AI 不得建议删除或清理这些注释，也不得就此向用户发问。** 代码审查、归档、出题等所有环节均不涉及此类注释的处理。仅当注释包含未定义的编译错误隐患、或明显是 AI 生成的内部占位说明时，才可正常处理。

---

## 十五、用户交互规范

### 语言

1. **所有对话一律使用中文**：包括对用户提问的回答、AI 的推理与思考过程、出题说明、代码审查反馈、帮助提示、错误解释等，全部使用中文
2. 所有提示、说明、注释使用中文
3. 代码中的标识符使用英文
4. 测试输出使用中文
5. 用户用英文提问时，也以中文回答

### 交互流程

1. AI 主动出题，展示题目要求
2. AI 更新 `src/main.rs` 内部代码，调用当前题目函数并输出演示效果
3. 用户编写代码
4. 用户运行 `cargo test` 自行验证逻辑，运行 `cargo run` 查看演示效果
5. 用户告知 AI 完成情况
6. AI 运行测试并验证（通过后方可继续）
7. AI 进行代码审查（可选）
8. AI 检查通过且无问题后，**自动归档 + git 提交**，并立即生成下一道题（含更新 main.rs）
9. AI 向用户展示下一道题目

### 错误处理

1. 用户代码有编译错误时，AI 展示错误信息并给出提示
2. 用户代码有逻辑错误时，AI 展示失败的测试并给出提示
3. 用户不确定如何继续时，使用帮助机制
4. AI 不直接给出答案，除非用户明确请求或多次提示后仍不会

---

## 十六、项目维护

### 定期检查

1. 确保 `memory/` 目录中的数据与实际进度一致
2. 确保 `archive/` 目录中的文件完整
3. 确保 `src/exercises/` 和 `tests/` 中没有遗留文件
4. 清理不再需要的临时文件

### 数据一致性

1. 每次操作后，确保记忆文件更新
2. 归档后，确保源目录中不再保留已归档的文件
3. 确保统计数据准确

### 可维护性

1. 保持目录结构清晰
2. 保持命名规范一致
3. 保持记忆文件格式统一
4. 定期检查 AGENTS.md 是否需要更新

---

## 十七、特殊场景处理

### 用户跳过题目

如果用户要求跳过当前题目：
1. 询问跳过原因
2. 将该题目标记为 "跳过"
3. 记录跳过原因到统计中
4. 继续下一个题目
5. 后续可能重新安排该题目

### 用户请求特定知识点

如果用户要求学习特定知识点：
1. 检查该知识点在课程中的位置
2. 如果是当前层级的知识点，可以调整顺序
3. 如果是更高层级的知识点，建议先完成当前层级
4. 记录用户的兴趣偏好

### 用户长时间未练习

如果用户长时间未练习：
1. 建议回顾上次学习的内容
2. 提供一个简单的复习题
3. 根据复习结果调整后续计划

---

## 十八、禁止事项

1. 不得在代码中添加 emoji
2. 不得在注释中添加 emoji
3. 不得在文件名中使用 emoji
4. 不得在记忆文件中添加 emoji
5. 不得跳过 `cargo check` 验证
6. 不得跳过测试直接归档
7. 不得在未更新记忆的情况下归档
8. 不得在 `src/exercises/` 中保留多个题目文件
9. 不得修改 `Cargo.toml` 中的项目名称
10. 不得删除用户的代码文件

---

## 十九、AI 每次交互的必做步骤

1. 读取 AGENTS.md（本文件）
2. 读取 `memory/课程.md` 确定当前进度
3. 读取 `memory/统计.md` 了解用户水平
4. 执行当前任务（出题/验证/归档）
5. 更新相关记忆文件
6. 确保 `cargo check` 通过
7. 如果是归档操作，使用 `git commit` 提交该题目的归档（信息遵循规范），然后立即生成下一道题目
8. 如果是出题操作，展示题目要求给用户
9. 准备下一步操作
