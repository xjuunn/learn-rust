// ============================================
// 题目编号: ex005
// 知识点: 基本类型综合
// 难度: 基础
// 所属章节: 01_基础语法
// ============================================

// 题目要求：
// 实现一个函数，接收一个 i32 年份，判断它是否为闰年。
//
// 闰年规则：
// - 能被 4 整除但不能被 100 整除的年份是闰年
// - 能被 400 整除的年份也是闰年
// - 其他年份不是闰年
//
// 示例：
// 输入：2000 → 输出：true（能被 400 整除）
// 输入：1900 → 输出：false（能被 100 整除但不能被 400 整除）
// 输入：2024 → 输出：true（能被 4 整除且不能被 100 整除）
// 输入：2023 → 输出：false
//
// 提示：取模运算符 % 可以判断整除，使用 && 和 || 组合条件

pub fn exercise_fn(year: i32) -> bool {
    // if year % 400 == 0 {
    //     return true;
    // } else if year % 4 == 0 && year %100 != 0 {
    //     return true;
    // } else {
    //     return false;
    // }
    (year % 400 == 0) || (year % 4 == 0 && year%100 != 0)
}
