// ============================================
// 题目编号: ex004
// 知识点: 函数定义与调用
// 难度: 基础
// 所属章节: 01_基础语法
// ============================================

// 题目要求：
// 实现一个函数，计算一个整数数组（&[i32]）中所有元素的和。
//
// 要求在函数内部定义一个辅助函数 sum_range，用于计算从 start 到 end（不含 end）区间的和，
// 然后主函数调用它来计算整个数组的和。
//
// 示例：
// 输入：&[1, 2, 3, 4, 5]
// 输出：15（1 + 2 + 3 + 4 + 5 = 15）
//
// 输入：&[-1, 5, 3]
// 输出：7（-1 + 5 + 3 = 7）
//
// 提示：数组切片可以用索引切片 a[start..end]，辅助函数 sum_range 需要接收切片参数

pub fn exercise_fn(a: &[i32]) -> i32 {
    // let sum_range = |nums:&[i32]| {
    //     let mut sum = 0;
    //     for i in nums {
    //         sum = sum + i;
    //     };
    //     sum
    // };
    // sum_range(a)

    fn sum_range(nums: &[i32]) -> i32 {
        let mut sum = 0;
        for &i in nums {
            sum += i;
        }
        sum
    }
    sum_range(a)
}
